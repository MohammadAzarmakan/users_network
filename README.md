# users_network
This is a tool that uses a graph consisting of users who are connected to each other and from which network information can be extracted.


How to build:
g++ -std=c++17 -O2 -mwindows main_gui.cpp -o friendnet.exe -lcomctl32 -lcomdlg32 -ldwmapi
